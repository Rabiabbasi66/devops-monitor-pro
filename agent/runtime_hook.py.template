"""PyInstaller runtime hook (GENERATED FILE - do not edit).

Generated by build_windows.ps1 from runtime_hook.py.template.
Embeds the production API URL selected in build_config.ps1 as a
frozen-app default. It can still be overridden at runtime with the
API_URL environment variable.

Contains NO secrets - only the public API endpoint URL.
"""
import os

# Frozen default API URL baked in at build time by build_windows.ps1.
_FROZEN_API_URL = "__API_URL__"

if getattr(__import__("sys"), "frozen", False) and _FROZEN_API_URL:
    if not os.environ.get("API_URL"):
        os.environ["API_URL"] = _FROZEN_API_URL
